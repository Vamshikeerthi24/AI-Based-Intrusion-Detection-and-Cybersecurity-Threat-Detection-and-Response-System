# Copy this file to `.env` and fill in values. Keep this file out of source control if it contains secrets.
# Example .env keys for OpenAI usage

# Your OpenAI API key (required for LLM explanations).
OPENAI_API_KEY=

# Optional: custom API base (e.g. internal proxy or Azure OpenAI endpoint)
# Example: https://api.openai.com/v1 or https://your-azure-openai-endpoint.openai.azure.com/
OPENAI_API_BASE=

# Default model to use for explanations. Leave blank to fall back to the project default.
# Examples: text-davinci-003, gpt-4, gpt-4o, gpt-4o-mini
OPENAI_DEFAULT_MODEL=text-davinci-003
